function argsPassed() {
  args="$1"
 if [[ "${args}" -ne 0 ]]; then 
  return 1
 fi
}

function usage() {
script_name=$(basename "$0")
cat <<EOF
./$script_name <arg1 arg2 arg3 ..>

eg: ./$script_name 1 2 3
EOF
exit 1
}
